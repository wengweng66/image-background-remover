name = "image-bg-oauth"
main = "index.js"
compatibility_date = "2024-01-01"

[vars]
# 生产环境变量（部署时在 Cloudflare Dashboard 设置）

[secrets]
# 在 Cloudflare Dashboard 添加以下 secrets:
# GOOGLE_CLIENT_ID = 你的 Client ID
# GOOGLE_CLIENT_SECRET = 你的 Client Secret